logging:
  config: classpath:logback-test.xml

spring:
  application:
    name: iki-pki-registration
  main:
    allow-bean-definition-overriding: true

app:
  id: ${spring.application.name}
apollo:
  meta: http://192.168.1.8:8080
  bootstrap:
    enabled: true
    namespaces: application.yml